[module/volume]
type = internal/alsa

# use default sink — works with both PulseAudio and PipeWire.
master-soundcard = default
master-mixer = Master

; scroll up/down to change volume.
interval = 5

# unmuted — FA6 Solid speaker glyphs via font-2 (T3 in polybar 1-based index).
format-volume = <ramp-volume> <bar-volume>
format-volume-padding = 1

# muted — FA6 Solid volume-xmark ().
format-muted = <label-muted>
label-muted = %{T3}%{T-} muted
format-muted-padding = 1
format-muted-background = ${colors.disabled}

# right-click opens pavucontrol.
click-right = pavucontrol &

# ramp — low / medium / high volume (FA6 Solid: , , ).
ramp-volume-0 = %{T3}%{T-}
ramp-volume-1 = %{T3}%{T-}
ramp-volume-2 = %{T3}%{T-}

# bar.
bar-volume-width = 8
bar-volume-foreground-0 = ${colors.primary}
bar-volume-gradient = false
bar-volume-indicator = |
bar-volume-fill = ─
bar-volume-empty = ─
bar-volume-empty-foreground = ${colors.disabled}
